# Data (downloaded manually from AIHub, never commit)
data/raw/
data/processed/
data/demo_samples/*
!data/demo_samples/.gitkeep

# Model weights / checkpoints
weights/

# Pipeline outputs (demo videos, benchmark artifacts)
outputs/

# Python
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# OS / editor
.DS_Store
Thumbs.db

# 외부 오픈소스는 third_party/ 아래 git submodule로 관리한다(.gitmodules 참고).
# 별도 clone을 만들 경우에만 아래 주석을 풀어 임시로 무시.
# third_party/
